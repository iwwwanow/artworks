📋 Пошаговый план изучения (16 недель)
Фаза 1: Инфраструктура и основы (2 недели)
Неделя 1-2: Монорепозиторий и бойлерплейт

text
monorepo/
├── packages/
│ ├── core/ # Общие утилиты (математика, экспорт)
│ ├── boilerplate/ # Базовый шаблон проекта
│ ├── project-1/ # Ваш первый проект
│ └── project-2/ # Второй проект
├── tools/ # Скрипты для сборки, экспорта
└── package.json # Workspaces конфигурация
Рекомендую:

Turborepo + pnpm workspaces (быстрее npm/yarn)

Vite для разработки (горячая перезагрузка, быстрая сборка)

Вынести общую логику в packages/core, а boilerplate сделать минимальным шаблоном

Бойлерплейт должен включать:

typescript
// Типизация для экспорта
interface ExportConfig {
format: 'png' | 'tiff' | 'gif' | 'svg';
resolution: number; // 300 для печати
size: { width: number; height: number };
filename: string;
}

// Базовый класс проекта
abstract class CreativeProject {
abstract setup(): void;
abstract draw(frame?: number): void;
export(config: ExportConfig): Promise<Blob>;
}
Фаза 2: Аналитическая геометрия + Canvas 2D (4 недели)
Неделя 3-4: Векторная алгебра и преобразования

Классы Vector2, Vector3, Matrix3, Matrix4

Линейные преобразования: translate, rotate, scale

Интерполяция кривые: линейная, квадратичная, кубическая

Неделя 5-6: Кривые и фигуры

Параметрические уравнения: окружность, эллипс, спираль

Кривые Безье (квадратичные, кубические)

Кривые Эрмита, сплайны Catmull-Rom

Неделя 7-8: Сложные геометрические алгоритмы

Вычислительная геометрия: пересечения, оболочки

Тесселяция, триангуляция Делоне

Диаграммы Вороного

Фаза 3: Работа с медиа и экспорт (2 недели)
Неделя 9: Canvas API продвинутый

OffscreenCanvas для worker'ов

ImageData и pixel manipulation

Gradients, patterns, clipping paths

Неделя 10: Экспорт и подготовка к печати

canvas.toBlob() с настройкой DPI

Библиотека ffmpeg.wasm для GIF

TIFF через UTIF.js или серверный рендеринг

Цветовые профили (sRGB, CMYK конвертация)

Фаза 4: Библиотеки и 3D (4 недели)
Неделя 11-12: p5.js + TypeScript

Интеграция @types/p5

Переход от чистого Canvas к p5

Интерактивность: мышь, клавиатура, тач

Неделя 13-14: Three.js основы

Scene, Camera, Renderer

Geometry, Material, Texture

Ортографическая и перспективная проекция

Неделя 15-16: Three.js продвинутый

Шейдеры (GLSL) + Three.js

Post-processing эффекты

CustomGeometry из математических функций

Фаза 5: Преобразование Фурье и генеративное искусство (4 недели)
Неделя 17-18: Основы DSP + Fourier

math.js или собственные реализации

FFT алгоритмы (Cooley-Tukey)

Визуализация: спектрограммы, эпициклоиды

Неделя 19-20: Анимация и сложные системы

Частицы с физикой

Реакционно-диффузионные системы (Turing patterns)

L-системы и фракталы

Фаза 6: Интеграция с вашими набросками (постоянно)
Параллельно с изучением:

Оцифровка эскизов: Создайте сканер или фотофиксацию

Векторизация: Используйте Potrace алгоритмы или ручное задание точек

Алгоритмическая интерпретация: Превращение эскизов в параметрические системы

🛠️ Техническая реализация
Core пакет (packages/core):
typescript
// Математические утилиты
export { Vector2, Vector3, Matrix3, Matrix4 };
export { BezierCurve, Spline, ParametricShape };

// Экспорт
export { exportCanvas, ExportConfig, PrintPreparer };

// Анимация
export { AnimationLoop, FrameExporter };

// Цвет
export { ColorSpace, CMYKConverter };
Конфигурация Vite для разных форматов:
javascript
// vite.config.js для проекта
export default {
build: {
lib: {
entry: 'src/main.ts',
formats: ['es', 'cjs']
},
rollupOptions: {
output: {
assetFileNames: (assetInfo) => {
if (assetInfo.name === 'style.css') return 'print.css';
return assetInfo.name;
}
}
}
}
}
Сервер Node.js для экспорта:
typescript
// tools/export-server.ts
import sharp from 'sharp';
import { GifEncoder } from 'gif-encoder';

async function exportForPrint(canvas: HTMLCanvasElement, config: ExportConfig) {
// Масштабирование до 300 PPI для 1080x1080
const scale = config.resolution / 96; // 96 DPI экрана
const width = canvas.width _ scale;
const height = canvas.height _ scale;

return await sharp(canvas.toBuffer('image/png'))
.resize(width, height, { fit: 'fill' })
.tiff({ compression: 'lzw' })
.toFile(`${config.filename}.tiff`);
}
📚 Ресурсы для изучения
Математика:
"Код творца" — отличная книга по мат. основам графики

"Nature of Code" Daniel Shiffman (есть на Processing/p5)

Freya Holmér YouTube канал (математика для графики)

Библиотеки:
p5.js: @types/p5, p5.js-svg для векторного экспорта

Three.js: @types/three, three-stdlib

Математика: math.js, gl-matrix

Экспорт: canvas-to-blob, jszip, gif.js

🎯 Первые практические шаги:
Сегодня: Настройте монорепозиторий с Turborepo

Завтра: Создайте packages/core с Vector2 и базовым Canvas рендерером

Неделя 1: Реализуйте бойлерплейт с экспортом PNG 300PPI

Неделя 2: Возьмите один простой набросок, оцифруйте его как набор точек

Неделя 3: Создайте алгоритмическую версию наброска с параметрами

💡 Советы по workflow:
Версионирование артов: Git LFS для больших TIFF файлов

Параметризация: Все изменяемые значения выносите в JSON конфиги

Тестирование печати: Делайте превью в PDF через pdfkit или jspdf

Производительность: Web Workers для тяжелых вычислений (Fourier, сложная геометрия)

Хотите, чтобы я подробнее расписал какой-то конкретный этап или помог с настройкой монорепозитория?
